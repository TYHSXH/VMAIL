# 使用说明

1. 刷新任务列表，选择“例7-10 改型全回转纯滚圆盘”。
2. 在控制页设置 crank_speed = -2 rad/s，再开始仿真。
3. 默认仿真时长为 3.2 s，曲柄会完成约一周的顺时针回转并自动暂停。
4. 从数据页读取 B、C 点的速度大小和加速度大小；需要分量时点击“导出当前数据”。
5. “计算并保存”会重新执行同一实验，并继承当前的 crank_speed 控制值。

修改后的尺寸为 OA=0.4 m、AB=1.8 m、R=1 m、r=0.5 m。曲线只显示核心的大小对比；导出的 CSV 中同时包含 x、y 分量。
