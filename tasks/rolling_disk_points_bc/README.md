# 使用说明

1. 刷新任务列表，选择“例7-10 曲柄驱动纯滚圆盘”。
2. 在控制页设置 crank_speed = -2 rad/s，再开始仿真。
3. 机构会从题图前约 0.1 rad 的姿态进入题图瞬时；当曲柄角经过 0 rad（约 t=0.05 s）时暂停。
4. 从数据页读取 B、C 点的速度大小和加速度大小；需要分量时点击“导出当前数据”。
5. “计算并保存”会重新执行同一短时实验，并继承当前的 crank_speed 控制值。

曲线只显示核心的大小对比；导出的 CSV 中同时包含 x、y 分量。
